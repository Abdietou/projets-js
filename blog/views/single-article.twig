{% extends "layout.twig" %}

{% block body %}
	<div class="" style="width: 100%;">
		<img src="https://picsum.photos/1800/300" class="card-img-top" alt={{article.name}}>
		<div class="card-body">
			<h3>{{article.name}}</h3>
			<a href="">Publié le{{article.publishedDate | date("d/m/Y")}}
			</a>
			<p class="card-text">{{article.content}}</p>
		</div>
	</div>
{% endblock %}
