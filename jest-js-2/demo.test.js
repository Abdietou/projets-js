describe('Pack Demo', () =>{

    // it ou test c'est la même chose
    test('Demo Some', () => {
        const a = 2 + 2;
        expect(a).toBe(4);
    });
    
    test('Demo Some 2', () => {
        const a = 2 / 2;
        expect(a).toBe(1);
    });

});